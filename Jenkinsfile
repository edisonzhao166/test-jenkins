pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    // Define a simple function
                    def helloWorld() {
                        return "Hello, World!"
                    }
                    // Call the function and print the result
                    echo helloWorld()
                }
            }
        }
    }
}
